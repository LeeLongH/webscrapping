services:
  webscrapping:
    image: dagjobswebscrapping:latest

    volumes:
      - ./AIRFLOW:/opt/airflow
      - ./requirements.txt:/tmp/requirements.txt

    ports:
      - "8080:8080"
    
    command: airflow standalone